<div class="wrapper">
  <h1 class="animation-tag">名古屋工業大学<br />プログラミング部</h1>
</div>

<style>
  .wrapper {
    display: block;
    width: 100%;
    position: relative;
  }
  h1 {
    text-align: center;
    font-weight: normal;
    min-height: calc(1.5em * 2);
  }
</style>
<script>
  import { ClubNameAnimation } from "./main";

  const className: string = ".animation-tag";
  const animationTag = document.querySelector<HTMLElement>(className);

  if (animationTag) {
    console.log(animationTag);
    ClubNameAnimation(animationTag);
  } else {
    throw new Error(className + "をもったタグは存在しません");
  }
</script>
